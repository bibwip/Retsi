<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.retsi.dabijhouder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DaBijhouder">
        <activity android:name="com.retsi.dabijhouder.EditOpdrachtActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name="com.retsi.dabijhouder.StartupActivity"
            android:screenOrientation="portrait"
            android:configChanges="locale|orientation"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen" />
        <activity
            android:name="com.retsi.dabijhouder.AddAssignmentAcitvity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">

        </activity>
        <activity android:name="com.retsi.dabijhouder.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:host="www.retsi.com"
                    android:scheme="http"/>
            </intent-filter>

        </activity>
    </application>

</manifest>